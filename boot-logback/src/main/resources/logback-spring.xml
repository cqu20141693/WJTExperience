<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- https://juejin.cn/post/6844903641535479821  -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <!--获取spring property-->
    <springProperty scope="context" name="springAppName" source="spring.application.name" defaultValue="default-app"/>
    <springProperty scope="context" name="logPath" source="log.path" defaultValue="./logs"/>

    <!-- 自定义属性转换器-->
    <conversionRule conversionWord="ip" converterClass="com.gow.logger.LogIpConfig"/>
    <!-- 定义属性-->
    <!-- 日志格式 -->
    <property name="FILE_LOG_PATTERN"
              value="${FILE_LOG_PATTERN:-(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} (${LOG_LEVEL_PATTERN:-%5p}) (${PID:- }){magenta} (---){faint} ([%15.15t]){faint} (%-40.40logger{39}){cyan} (:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
    <property name="LOG_FILE" value="${logPath}"/>
    <!-- 控制台日志appender  -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>debug</level>
        </filter>
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>
    <!-- Appender to log to file -->
    <appender name="flatFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>
        <prudent>false</prudent>
        <file>${LOG_FILE}/${springAppName}/${springAppName}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}/${springAppName}/${springAppName}.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <!-- 单个日志文件最大大小，当文件达到该大小则触发截断（以及压缩）-->
            <maxFileSize>10MB</maxFileSize>
            <!-- 日志文件保留最大时间滚动周期，比如当filaNamePattern中%d以为dd结尾时，则保留60天-->
            <maxHistory>15</maxHistory>
            <!-- 日志文件保留的总的最大大小-->
            <totalSizeCap>20GB</totalSizeCap>

        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>
    <!-- Appender to log to file -->
    <appender name="debugFlatFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="com.witeam.log.filter.FixLevelFilter">
            <level>debug</level>
        </filter>
        <prudent>false</prudent>
        <file>${LOG_FILE}/${springAppName}/debug/${springAppName}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}/${springAppName}/debug/${springAppName}.log.%d{yyyy-MM-dd}.%i.gz
            </fileNamePattern>
            <!-- 单个日志文件最大大小，当文件达到该大小则触发截断（以及压缩）-->
            <maxFileSize>10MB</maxFileSize>
            <!-- 日志文件保留最大时间滚动周期，比如当filaNamePattern中%d以为dd结尾时，则保留60天-->
            <maxHistory>15</maxHistory>
            <!-- 日志文件保留的总的最大大小-->
            <totalSizeCap>20GB</totalSizeCap>

        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>
    <!-- Appender to log to file in a JSON format -->
    <appender name="filebeat" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>info</level>
        </filter>
        <file>${LOG_FILE}/${springAppName}/${springAppName}.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}/${springAppName}/${springAppName}.json.%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>5</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                        "severity": "%level",
                        "service": "${springAppName:-}",
                        "XForwardedFor":"%X{X-Forwarded-For:-}",
                        "trace": "%X{X-B3-TraceId:-}",
                        "span": "%X{X-B3-SpanId:-}",
                        "parent": "%X{X-B3-ParentSpanId:-}",
                        "exportable": "%X{X-Span-Export:-}",
                        "pid": "${PID:-}",
                        "thread": "%thread",
                        "class": "%logger{40}",
                        "rest": "%message %ex{6}"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>
    <!--    独立配置日志组件-->
    <logger name="com.witeam.log.debugComponent">
        <appender-ref ref="debugFlatFile"/>
    </logger>
    <!--    默认配置了所有包的level,每个logger可以独立配置level-->
    <root level="INFO">
        <appender-ref ref="console"/>
        <appender-ref ref="flatFile"/>
        <!--        <appender-ref ref="filebeat"/>-->
        <springProfile name="test,prod">
            <appender-ref ref="filebeat"/>
        </springProfile>
    </root>
</configuration>
